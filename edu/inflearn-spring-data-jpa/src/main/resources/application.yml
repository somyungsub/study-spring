spring:
  datasource:
    url: jdbc:postgresql://localhost:6432/springdatajpa
    username: ssosso
    password: ssosso
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
